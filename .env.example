# Copy to .env.local and fill in values. Never commit .env.local.

# Supabase (Phase 1+)
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Add other vars as features land; validate in src/lib/env.ts per AGENTS.md.
