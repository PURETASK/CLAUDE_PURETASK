'use client';

import { Button } from '@/components/ui/button';

export const CheckrLauncher = () => {
  return (
    <Button type="button" variant="secondary">
      Submit background check (stub)
    </Button>
  );
};
